export interface LocationOption {
  id: string;
  name: string;
  district: string;
  province: string;
  latitude: number;
  longitude: number;
}

export interface LocationPlace {
  id: string;
  name: string;
  main_text: string;
  secondary_text: string;
}
